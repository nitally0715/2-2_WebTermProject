{
  "C++": [
    "std::vector<int> scores = {1, 2, 3};",
    "for(auto &item : container) { process(item); }",
    "class Player { public: std::string name; };",
    "constexpr int kBufferSize = 256;",
    "std::unique_ptr<Logger> logger = std::make_unique<Logger>();",
    "template<typename T> void Swap(T& a, T& b) { T tmp = a; a = b; b = tmp; }",
    "enum class Difficulty { Easy, Normal, Hard };",
    "std::optional<int> result = MaybeCompute();",
    "std::sort(values.begin(), values.end());",
    "if(stream.fail()) { throw std::runtime_error(\"failed\"); }"
  ],
  "Java": [
    "List<String> users = new ArrayList<>();",
    "@Override public String toString() { return name; }",
    "try (var reader = Files.newBufferedReader(path)) { ... }",
    "record Score(String user, int value) {}",
    "CompletableFuture.runAsync(() -> sync());",
    "Stream.of(items).filter(Objects::nonNull).toList();",
    "if(response.isEmpty()) { return Optional.empty(); }",
    "Map<String, Integer> cache = new HashMap<>();",
    "var now = Instant.now();",
    "public sealed interface Command permits Start, Stop {}"
  ],
  "Python": [
    "numbers = [n for n in range(10) if n % 2 == 0]",
    "config: dict[str, str] = {\"theme\": \"dark\"}",
    "with open(\"log.txt\", \"a\", encoding=\"utf-8\") as handler:",
    "def greet(name: str) -> str: return f\"Hello {name}\"",
    "from dataclasses import dataclass",
    "@dataclass class Point: x: int; y: int",
    "match event: case {\"type\": \"start\"}: handle()",
    "total = sum(values) if values else 0",
    "paths = sorted(Path(\".\").glob(\"*.py\"))",
    "if __name__ == \"__main__\": main()"
  ]
}
